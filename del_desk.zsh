# Delete your desktop
defaults write com.apple.finder CreateDesktop false; killall Finder (supprimer les docs sur le desktop)
# or 
defaults write com.apple.finder CreateDesktop true; killall Finder (afficher les docs sur le desktop)